---
import { footerNavigation, labels, localizedHome, site } from '../data/site';
import type { Language } from '../data/types';
interface Props { lang: Language; }
const { lang } = Astro.props;
const t = labels[lang];
const year = new Date().getFullYear();
---
<footer class="site-footer">
  <div class="shell footer-grid">
    <div class="footer-lead"><a href={localizedHome(lang)} class="footer-logo"><img src="/brand/isarklima-logo.svg" width="258" height="55" alt="IsarKlima" /></a><p>{lang === 'de' ? 'Planung und Montage von Split-Klimaanlagen für Wohnungen, Häuser und kleinere Gewerbeflächen.' : 'Split air conditioning installation for apartments, homes and small commercial spaces.'}</p><span>{site.serviceArea[lang]}</span></div>
    <nav aria-label={lang === 'de' ? 'Fußnavigation' : 'Footer navigation'}>{footerNavigation[lang].map(([href, label]) => <a href={href}>{label}</a>)}</nav>
    <div class="footer-status"><span class="status-dot" aria-hidden="true"></span><div><strong>{t.preview}</strong><p>{t.previewText}</p></div></div>
  </div>
  <div class="shell footer-bottom"><p>© {year} IsarKlima</p><p>{lang === 'de' ? 'Bildmaterial: illustrative Darstellungen, keine Referenzprojekte.' : 'Imagery: illustrative visuals, not project references.'}</p></div>
</footer>
